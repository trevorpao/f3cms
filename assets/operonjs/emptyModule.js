;(function(app, gee, $){
    "use strict";

    // register a module name
    app.moduleName = {
    };

    // hook some handler
    gee.hook('actionName', function(me){
    });

}(app, gee, jQuery));
